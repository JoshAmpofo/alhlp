#!/usr/bin/node

// This script will update a value using the object notation in JS

const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);

myObject.value = 89;
console.log(myObject);
